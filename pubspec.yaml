name: feild_survey_app
description: "A professional distributed survey platform."
version: 1.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.4+4
  sqflite_common_ffi_web: ^1.1.1
  path: ^1.8.3
  http: ^1.2.2
  flutter_secure_storage: ^9.2.2
  connectivity_plus: ^6.1.0
  mobile_scanner: ^7.2.0
  qr_flutter: ^4.1.0
  csv: ^6.0.0
  fl_chart: ^0.71.0
  uuid: ^4.0.0
  
  # Professional Firebase Stack (Compatible Versions)
  firebase_core: ^3.1.1
  cloud_firestore: ^5.0.2
  firebase_auth: ^5.1.2
  firebase_storage: ^12.1.2
  
  geolocator: ^13.0.1
  google_maps_flutter: ^2.10.0
  image_picker: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
